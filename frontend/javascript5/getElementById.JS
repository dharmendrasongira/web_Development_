let ok = document.getElementById("mainImg");

// The 'getElementById' method returns the element that has the ID "mainImg".
// 'console.dir(ok)' is used to log a detailed interactive list of the element's properties and methods, 
// allowing you to inspect the DOM object in the console.
console.dir(ok);
