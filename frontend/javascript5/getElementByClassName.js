let op = document.getElementsByClassName("box");

// The 'getElementsByClassName' method returns an HTMLCollection of all elements in the document with the class name "box".
// Since it returns an array-like object, 'console.dir(op)' is used to log the detailed properties and structure of the HTMLCollection object.
// This helps to inspect the elements and their attributes in the console.
console.dir(op);
