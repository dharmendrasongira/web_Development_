const sum =(a,b) => {
    console.log(a+b);
};

// agar  argument single hai toh bina () ke bhi use kar skte hai
const cube= a=>{ 
    return(a*a*a);
};


// or agar bina return satement ke bhi use kar skte hai agar single experssion hoto
const mul= (a,b) => a*b;